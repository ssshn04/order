CREATE TABLE `t_dogs`
(
    `id` bigint(20) NOT NULL AUTO_INCREMENT,
    `name` varchar(255),
    `kind` varchar(255),
    'age' int(10),
    'likes' int(100),
    'sex' varchar(50),
    PRIMARY KEY (`id`)
    );